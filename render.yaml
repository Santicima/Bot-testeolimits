services:
  - type: worker
    name: stake-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y chromium chromium-driver
    startCommand: python bot.py
